<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.mybatis.test.dao.IUserDao">

<!--    <select id="queryUserInfoById" parameterType="java.lang.Long" resultType="cn.bugstack.mybatis.test.po.User">
        SELECT id, userId, userName, userHead
        FROM user
&lt;!&ndash;        <trim prefix="where" prefixOverrides="AND | OR" suffixOverrides="and">&ndash;&gt;
&lt;!&ndash;            <if test="null != uId">&ndash;&gt;
&lt;!&ndash;                id = #{uId}&ndash;&gt;
&lt;!&ndash;            </if>&ndash;&gt;
&lt;!&ndash;        </trim>&ndash;&gt;
        where id = #{uId}
    </select>-->

    <select id="queryUserInfo" parameterType="cn.bugstack.mybatis.test.po.User" resultType="cn.bugstack.mybatis.test.po.User">
        SELECT id, userId, userName, userHead
        FROM user
        where id = #{id}
    </select>

</mapper>
